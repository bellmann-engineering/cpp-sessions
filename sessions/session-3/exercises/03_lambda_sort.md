# Übung 3: Lambda für benutzerdefiniertes Sortieren

**Ziel:** Verwenden Sie ein Lambda, um ein Array von Strukturen nach verschiedenen Kriterien zu sortieren.

## Aufgabenstellung

Gegeben ist folgende Struktur:

```cpp
struct SensorData {
    int id;
    float value;
    uint32_t timestamp;
};
